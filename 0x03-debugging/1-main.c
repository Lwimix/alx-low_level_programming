#include <stdio.h>

/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;
	/* Comments are in blue */
	printf("Infinite loop incoming :(\n");
	/**
	 * i = 0;
	 *
	 * while (i < 10)
	 * {
	 *	putchar(i);
	 * }
	 */
	printf("Infinite loop avoided! \\o/\n");
	/* They make this look colorful */
	return (0);
}
