#include "main.h"

/**
 * _strcat - put strings together
 * @dest: destination point
 * @src: source string
 *
 * Return: added string
 */
char *_strcat(char *dest, char *src)
{
	dest = src;
	return (dest);
}
