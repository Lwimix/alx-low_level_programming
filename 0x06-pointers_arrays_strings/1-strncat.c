#include "main.h"

/**
 * _strncat - adds two strings 
 * @dest: destination point
 * @src: source string
 * @n: number of bytes
 *
 * Return: added string
 */
char *_strncat(char *dest, char *src, int n)
{
	int i;

	for (i = 0; i < n; i++)
	{
		if (dest[i] == '\0')
	}
	if (dest[i] == '\0')
	{
	}
	return (dest);
}
