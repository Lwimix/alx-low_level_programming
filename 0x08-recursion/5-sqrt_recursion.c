#include "main.h"

/**
 * _sqrt_recursion - square root of a number
 * @n: number input
 *
 * Return: value of integer
 */
int _sqrt_recursion(int n)
{
	int count = 1, i = 2, u;
	
	u = n;
	if (u == 0)
	{}
}
